default:
  enabled: true
  server_url: <%= ENV["REDISCLOUD_URL"] %>
  client_url: <%= ENV["REDISCLOUD_URL"] %>

development:

test:

production:
  # If you want to use sidekiq, require job dyno
  enabled: false
